# Spring Boot configuration
spring:
  # Database
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:oddshub_db
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect