# Spring Boot configuration
spring:
  # Database
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:oddshub_db
    username: sa
    password: password
  liquibase:
    enabled: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  # Java Mail Config
  mail:
    host: smtp.gmail.com
    port: 587
    username: tamx251459@gmail.com
    password: lxtcaewwzvmmfnqj
    properties:
      mail:
        transport:
          protocol: smtp
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true